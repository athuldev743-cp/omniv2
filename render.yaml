services:
  - type: web
    name: neolix-baileys
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: AIVEN_DATABASE_URL
        sync: false
      - key: AIVEN_CA_CERT
        sync: false